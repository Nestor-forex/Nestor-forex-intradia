name: Reporte diario del barrido intradía

on:
  schedule:
    - cron: '0 13 * * 1-5' # 8:00 a.m. hora de Colombia (UTC-5), lunes a viernes
  workflow_dispatch:

jobs:
  reporte:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Generar reporte
        working-directory: app
        env:
          # Si el secreto no está puesto, velas.mjs se cae a la llave de
          # .env.production y todo sigue igual.
          TWELVEDATA_KEY: ${{ secrets.TWELVEDATA_KEY }}
        run: node scripts/reporte-diario.mjs
